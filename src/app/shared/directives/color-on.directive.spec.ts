import { ColorOnDirective } from './color-on.directive';

describe('ColorOnDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorOnDirective();
    expect(directive).toBeTruthy();
  });
});
