import { ColorOffDirective } from './color-off.directive';

describe('ColorOffDirective', () => {
  it('should create an instance', () => {
    const directive = new ColorOffDirective();
    expect(directive).toBeTruthy();
  });
});
